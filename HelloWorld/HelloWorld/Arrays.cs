using System;
using System.Collections.Generic;
using System.Text;

namespace HelloWorld
{
    class Arrays
    {
        void Demo()
        {
            var numbers = new int[3];
            numbers[0] = 1;
            var flags = new bool[3];
            flags[0] = true;
            var names = new string[3] { "Jack", "John", "Ville" };
        }
        
    }
}
